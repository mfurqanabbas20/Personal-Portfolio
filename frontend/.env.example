# Resend API key — get one at https://resend.com/api-keys
# Server-only: NOT prefixed with VITE_, so it is never exposed to the browser.
RESEND_KEY=your_resend_api_key_here
# Optional overrides (defaults live in api/_handler.js):
# CONTACT_TO=mfurqanabbas20@gmail.com
# CONTACT_FROM=Portfolio Contact <onboarding@resend.dev>
